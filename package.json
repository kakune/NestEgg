{
  "name": "nestegg",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "test": "npm run test -w backend && npm run test -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "lint:fix": "npm run lint:fix -w backend && npm run lint:fix -w frontend",
    "prepare": "husky install || true",
    "e2e:install": "playwright install",
    "e2e:open": "playwright test --ui",
    "e2e:run": "playwright test",
    "e2e:docker": "PLAYWRIGHT_BASE_URL=http://localhost:8080 docker compose -f docker-compose.e2e.yml up --build -d && sleep 10 && PLAYWRIGHT_BASE_URL=http://localhost:8080 playwright test && docker compose -f docker-compose.e2e.yml down"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md,css,scss}": [
      "prettier --write"
    ],
    "backend/**/*.{ts,tsx}": [
      "cd backend && npm run lint:fix"
    ],
    "frontend/**/*.{ts,tsx}": [
      "cd frontend && npm run lint:fix"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.6.2"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
